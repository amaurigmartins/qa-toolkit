Inspect the enrolled repository and its accepted plan. Propose the exact values required by `qat work init`: repository, issue, remote, base branch and SHA, work ID, task ID and title, implementation branch, expected parent, final subject, literal allowed paths, argv-only validation, and check or Sentinel proof. Do not create or mutate state until the user accepts the proposal.

