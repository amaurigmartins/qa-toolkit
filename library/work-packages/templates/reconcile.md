# Reconciliation note

Record the observed local HEAD, remote branch SHA, structured phase, provisional SHA, final SHA, and any identity mismatch. Recovery is performed by `qat work reconcile`, never by editing state JSON or using an unbounded force push.

