# Final cleanup task

List only reconciliation residue to remove and the Sentinel acceptance required before `qat work retire`. Retirement removes local structured state; it does not merge, tag, release, or close remote records.

