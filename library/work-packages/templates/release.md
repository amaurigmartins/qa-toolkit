# Separate release task

Record the merged implementation revision, calculated SemVer increment, exact version and release-note paths, release branch and base, and required validation. Tagging and release publication require separate authorization.

