schema_version = 1
profile = "qa-toolkit"
native_configurations = ["pyproject.toml"]
protected_paths = ["registry/tools.json", "profiles/qa-toolkit.toml"]

[vocabulary]
additions = []
allowances = []

[work]
state_directory = ".git/qat/work"
require_allowed_paths = true

